# Legacy PHP Backend

The active legacy backend is currently located in project root:
- `api/`
- `sql/`

This folder exists to mark migration intent. During migration, move module by module into `backend/v1` while keeping legacy endpoints stable until mobile app switches fully.
